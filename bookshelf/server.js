// // this will be our express web server backend

// require("dotenv").config();

// const { Environment } = require("@angular/compiler-cli/src/ngtsc/typecheck/src/environment");
// const express = require("express")

// // Same as above in some instances  ->  import express from "express"

// const app = express();

// const root = "./dist/book-app"

// app.use(express.static(root));

// app.get("/*", (req, res) =>
//   res.sendFile("index.html", {root}));

//   console.process(process.env.SOME_HIDDEN_KEY);

// app.listen(process/Environment.PORT || 8080);

